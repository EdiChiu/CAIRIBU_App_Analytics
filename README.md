# CAIRIBU_App_WebScraper
This is a script to grab CAIRIBU events and update the Firestore database

# Command to run the script:

```bash 
npm install # install dependencies first if you haven't done so
node index.js
```